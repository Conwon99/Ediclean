---
import Layout from '../layouts/Layout.astro';
import { Navbar } from '../sections/Navbar';
import { ServicePageHero } from '../sections/ServicePageHero';
import { ServicePageServices } from '../sections/ServicePageServices';
import { QuoteSection } from '../sections/QuoteSection';
import { FooterSection } from '../sections/FooterSection';
import { BUSINESS_NAME, GBP_PRIMARY_CATEGORY_LABEL, SERVICE_REGION_SHORT, SITE_TITLE_AREA } from '../constants/site';
---

<Layout 
  title={`${GBP_PRIMARY_CATEGORY_LABEL} — Full List | ${SITE_TITLE_AREA} | ${BUSINESS_NAME}`}
  description={`Complete ${GBP_PRIMARY_CATEGORY_LABEL} list for ${SERVICE_REGION_SHORT}: roof cleaning, render softwashing, driveways, gutters, PVC and windows. Free quotes.`}
>
  <div class="min-h-screen bg-neutral-100 font-figtree text-black text-sm leading-5 antialiased">
    <Navbar client:load />
    <main class="pt-0">
      <ServicePageHero client:load />
      <ServicePageServices client:load />
      <QuoteSection client:load showTerms={false} />
      <FooterSection client:load />
    </main>
  </div>
</Layout>
